@model IEnumerable<FreshDrink.Web.ViewModels.OrderSummaryVm>

@{
    ViewData["Title"] = "Đơn hàng";
    bool isAdmin = User.IsInRole("Admin");
}

<div class="container py-4">

    <h3 class="fw-bold mb-4">📦 Danh sách đơn hàng</h3>

    @if (!Model.Any())
    {
        <div class="alert alert-warning text-center p-4 rounded-3 shadow-sm">
            ⚠ Chưa có đơn hàng nào.
            <br />
            <a asp-controller="Store" asp-action="Index" class="btn btn-primary mt-3 rounded-pill px-4">
                🛍 Mua sắm ngay
            </a>
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-hover table-bordered rounded-3 overflow-hidden bg-white">
                <thead class="bg-light">
                    <tr class="text-muted small text-center">
                        <th>Mã đơn</th>
                        @if (isAdmin)
                        {
                            <th>Người đặt</th>
                        }
                        <th>Tổng tiền</th>
                        <th>Trạng thái</th>
                        <th>Ngày tạo</th>
                        <th>Hành động</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var o in Model)
                    {
                        string badgeColor = o.Status switch
                        {
                            "Pending" => "warning",
                            "Approved" => "success",
                            "Rejected" => "danger",
                            _ => "secondary"
                        };

                        <tr class="align-middle text-center">
                            <td class="fw-bold">#@o.Id</td>

                            @if (isAdmin)
                            {
                                <td>@o.UserName</td>
                            }

                            <td>@o.Total.ToString("N0") đ</td>

                            <td>
                                <span class="badge bg-@badgeColor px-3 py-2 rounded-pill">
                                    @o.Status
                                </span>
                            </td>

                            <td>@o.CreatedAt.ToString("dd/MM/yyyy HH:mm")</td>

                            <td>
                                <div class="d-flex justify-content-center gap-2">

                                    <!-- Xem -->
                                    <a asp-action="Details"
                                       asp-route-id="@o.Id"
                                       class="btn btn-outline-primary btn-sm rounded-pill px-3">
                                        👁 Xem
                                    </a>

                                    @if (isAdmin && o.Status == "Pending")
                                    {
                                        <!-- DUYỆT -->
                                        <form asp-action="Approve" method="post">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@o.Id" />
                                            <button class="btn btn-success btn-sm rounded-pill px-3">
                                                ✔ Duyệt
                                            </button>
                                        </form>

                                        <!-- TỪ CHỐI -->
                                        <form asp-action="Reject" method="post">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@o.Id" />
                                            <button class="btn btn-warning btn-sm rounded-pill px-3">
                                                ⚠ Từ chối
                                            </button>
                                        </form>
                                    }

                                    @if (isAdmin)
                                    {
                                        <!-- XÓA -->
                                        <form asp-action="Delete" method="post" onsubmit="return confirm('Bạn có chắc muốn xóa đơn hàng #@o.Id?');">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@o.Id" />
                                            <button class="btn btn-outline-danger btn-sm rounded-pill px-3">
                                                🗑 Xóa
                                            </button>
                                        </form>
                                    }

                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>

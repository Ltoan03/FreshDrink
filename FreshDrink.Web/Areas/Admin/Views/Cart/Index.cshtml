@model IEnumerable<FreshDrink.Data.Models.Order>

<h2 class="mb-3">📦 Quản lý đơn hàng</h2>

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Người mua</th>
            <th>Trạng thái</th>
            <th>Số SP</th>
            <th>Tổng tiền</th>
            <th>Ngày tạo</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var o in Model)
        {
            <tr>
                <td>@o.Id</td>

                <td>
                    @(o.User != null 
                        ? (o.User.FullName ?? o.User.Email ?? "Không rõ") 
                        : "Không rõ")
                </td>

                <td>@o.Status</td>

                <td>@o.Items?.Count() ?? 0</td>

                <td>@String.Format("{0:N0}đ", o.Total)</td>

                <td>@o.CreatedAt.ToString("dd/MM/yyyy")</td>
            </tr>
        }
    </tbody>
</table>
